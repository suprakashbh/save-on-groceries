VITE_COGNITO_DOMAIN=us-east-1btj0kck5q.auth.us-east-1.amazoncognito.com
VITE_COGNITO_CLIENT_ID=6vt01hrj1d8u7pln5j5dpf56rp
# If omitted, the app uses window.location.origin + /callback.html
# Keep this unset for deployments so the app uses the current site origin.
# VITE_COGNITO_REDIRECT_SIGNIN=https://localhost:5001/callback.html
# If omitted, the app uses window.location.origin + /
# Keep this unset for deployments so the app uses the current site origin.
# VITE_COGNITO_REDIRECT_SIGNOUT=https://localhost:5001/
VITE_COGNITO_SCOPES=openid email profile
VITE_API_BASE_URL=https://2xtv24ztwk.execute-api.us-east-1.amazonaws.com/poc/chat
